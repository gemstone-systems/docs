# typescript node template

this template is for a backend node server in typescript.

## usage

1. clone this repo `git clone https://github.com/NekoDrone/nix-templates`
2. copy the template `cp -r nix-templates/typescript-node-template path/to/destination/`
3. rename the template accordingly. this readme, the `package.json`, `default.nix`, etc.
